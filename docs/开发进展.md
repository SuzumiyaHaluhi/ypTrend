# ypTrend 开发进展

更新时间：2026-04-16

## 当前阶段结论
- Web MVP 主链路稳定可用，在线监控能力闭环已完成。
- 新版自包含 Skill `hot-monitor` 已完成并通过可用性验证。
- 项目从“仅 Web 版”升级为“Web 平台 + 自包含 Skill”双轨交付。

## 已完成（代码已落地）

### 1. Web 平台能力（保持稳定）
- 监控规则管理、手动触发扫描、热点流/通知流展示已完成。
- 多源采集 -> AI 判定 -> 门槛拦截 -> 飞书通知 -> SSE 推送链路已闭环。
- 评分与门槛策略（Twitter/Web/RSS）已参数化并可在页面配置。

### 2. 数据与接口增强（保持生效）
- `hot_items` 结构化字段已落库并自动迁移兼容旧库。
- `/api/hot-items`、`/api/notifications`、SSE 事件均已输出结构化质量字段与 AI 字段。

### 3. 自包含 Skill 重构（本轮完成）
- 已抛弃旧方案目录：`skills/yptrend-hotspot-monitor`。
- 已重建新方案目录：`skills/hot-monitor`。
- 已交付文件：
  - `SKILL.md`
  - `agents/openai.yaml`
  - `references/search-sources.md`
  - `references/analysis-guide.md`
  - `scripts/search_web.py`
  - `scripts/search_china.py`
  - `scripts/search_twitter.py`
  - `scripts/generate_report.py`

### 4. Skill 功能特性
- 不依赖本地后端服务。
- 直接调用外部源/API 采集热点。
- Twitter Key 可选，缺失时自动跳过。
- 统一合并 JSON 并生成 Markdown 报告。
- 通过分析指南输出可解释结论。

## 验收状态

### Web 平台
- 后端：`npm --prefix server test` 通过。
- E2E：`npm --prefix server run accept:p0-1` 通过。
- 持久化：`npm --prefix server run accept:p0-2` 通过。
- 前端：`npm --prefix web run lint`、`npm --prefix web run build` 通过。

### 自包含 Skill
- `quick_validate.py`：通过。
- 4 个 Python 脚本 `--help` 冒烟：通过。
- `search_twitter.py` 无 Key 场景：通过（返回 skipped，不阻塞）。

## 本轮关键变更（2026-04-16）
1. 明确双轨架构并更新文档基线为 v1.6。
2. 完成旧 Skill 下线与新 Skill 切换。
3. 完成自包含采集脚本与报告脚本首版。
4. 完成 Skill 验证与可执行性检查。

## 仍在进行 / 下一步
1. 强化中文数据源稳定性与异常兜底策略。
2. 将报告升级为“优先级 + 风险 + 动作建议”的决策看板格式。
3. 增加可选通知通道（飞书/邮件）而不引入本地后端耦合。
